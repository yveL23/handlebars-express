<div class="book-page">
    <a href="/books">Voltar</a>

    <div class="container">
        <h1>{{books.title}}</h1>
        <p>O livro tem {{books.numero_paginas}} Páginas.</p>
    </div>

    <div>
        <a href="/books/edit/{{books.id}}">Editar</a>
        <form action="/books/remove/{{books.id}}" method="post">
            <input type="submit" value="Remover">
        </form>
    </div>
</div>